{
  "name": "watch-time-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-browser extension that tracks video watch time and shows local analytics.",
  "scripts": {
    "build": "node scripts/build.js",
    "pack": "node scripts/build.js && node scripts/pack.js",
    "web-ext:firefox": "npm run build && web-ext run --source-dir=dist/firefox"
  },
  "devDependencies": {
    "web-ext": "^8.3.0"
  }
}
